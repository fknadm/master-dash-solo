import React from "react";
import "../App.css"
const Hero = () => (
  <div style={{fontSize:"18px"}} className="text-center hero my-5">
    <h1 className="mb-4">Master Dashboard</h1>

    <p style={{fontSize:"18px"}} className="lead">
      This is a Dashboard for XERO PRIME / FOUNDING BROKERS / IMCS usage to track inter-dept. ticketing, requests and overall client performance.
    </p>
  </div>
);

export default Hero;
